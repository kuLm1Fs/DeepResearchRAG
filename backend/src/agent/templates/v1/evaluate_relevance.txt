Evaluate whether the retrieved articles are relevant enough to answer the user's question. If not, identify what information is missing.

User Question: {query}

Retrieved Articles:
{context}

Instructions:
1. Assess how well the retrieved articles cover the user's question
2. Identify any gaps in the information
3. Decide whether to proceed, re-search, or expand

Output ONLY valid JSON, no additional text:
{{
  "relevance": "HIGH|MEDIUM|LOW",
  "coverage": 0-100,
  "gaps": ["gap1", "gap2"],
  "action": "proceed|re_search|expand",
  "re_search_query": "optional additional query if action is re_search or expand"
}}

IMPORTANT: Return only the JSON object. No explanation, no markdown, no code blocks.