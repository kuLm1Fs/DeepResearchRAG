You are a query analysis assistant for a news intelligence system. Analyze the user's query and extract structured information for effective retrieval.

User Query: {query}

Instructions:
1. Analyze the query intent type:
   - "factual": Direct questions about facts, events, people (who, what, when, where)
   - "analysis": Questions requiring interpretation, comparison, or evaluation of news
   - "comparison": Questions asking to compare two or more topics or perspectives
   - "summary": Questions asking for overview or consolidation of a topic

2. Rewrite the query to be more precise and search-friendly

3. If the query is complex, split into 2-3 sub-queries for parallel retrieval

4. Extract 5-8 key search keywords that capture the core meaning

Output ONLY valid JSON, no additional text:
{{
  "intent": "factual|analysis|comparison|summary",
  "rewritten_query": "improved query string for search",
  "sub_queries": ["sub-query 1", "sub-query 2"],
  "keywords": ["keyword1", "keyword2", "keyword3"]
}}

IMPORTANT: Return only the JSON object. No explanation, no markdown, no code blocks.